<div class="band-content group">
		<section class="band-content-main-index">
		<div class="all-bands">
			<% bands.each(function (band) { %>
				<div class="band-container">
					<div class="band-image group">
						<a href="#/bands/<%= band.id %>">
							<img src="<%= band.escape('image') %>">
						</a>
						
					</div>
					<div class="band-info">
						<%= band.escape("name") %>
					</div>
				</div>
			<% }) %>
		</div>
  </section>
</div>

